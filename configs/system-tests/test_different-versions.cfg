{
    "params": {
	"elliptics_old_version": "2.25.6.10",
	"eblob_old_version": "0.22.0"
        },
    "addopts": "--old-version={{ elliptics_old_version }}{% for node in servers %} --node={{ node.host }}:{{ node.port }}:{{ node.group }}{% endfor %}",
    "dir": "system-tests/different-versions",
    "test_env_cfg": {
        "clients": {
            "count": 2,
            "flavor": "m1.small"
            },
        "servers": {
            "count_per_group": [2, 2],
            "flavor": "m1.small"
            },
	"setup_playbook": "tests/setup-different-versions-test",
	"teardown_playbook": "tests/teardown-different-versions-test"
        },
    "tags": ["system", "different-versions", "testing-packages"]
}
