{
    "params": {
	"blob_size": 314572800
        },
    "addopts": "--timeout=3600 --files_number=512 --files_size=0 -m \"merge\"{% for node in servers %} --node={{ node.host }}:{{ node.port }}:{{ node.group }}{% endfor %}",
    "dir": "system-tests/recovery-tests",
    "test_env_cfg": {
        "clients": {
            "count": 1,
            "flavor": "m1.medium"
            },
        "servers": {
            "count_per_group": [4],
            "flavor": "m1.small"
            },
	"setup_playbook": "elliptics-start",
	"teardown_playbook": "elliptics-stop"
        },
    "tags": ["recovery-test", "system", "merge", "testing-packages"]
}
