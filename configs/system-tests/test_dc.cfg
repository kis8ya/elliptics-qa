{
    "params": {
	"blob_size": 314572800,
	"cache_sync_timeout": 30
        },
    "addopts": "{% for node in servers %} --node={{ node.host }}:{{ node.port }}:{{ node.group }}{% endfor %} --file-size=102400 --consistent-files-number=256 --inconsistent-files-number=1000 --inconsistent-files-percentage=0.33 --cache-sync-timeout={{ cache_sync_timeout }}",
    "dir": "system-tests/recovery-tests/dc",
    "test_env_cfg": {
        "clients": {
            "count": 1,
            "flavor": "m1.medium"
            },
        "servers": {
            "count_per_group": [2, 2, 2],
            "flavor": "m1.small"
            },
	"setup_playbook": "tests/setup-recovery-test",
	"teardown_playbook": "elliptics-stop"
        },
    "tags": ["recovery-test", "system", "recovery-dc", "testing-packages"]
}
