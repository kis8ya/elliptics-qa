{
    "type": "pytest",
    "target": "system-tests/cache-tests/overhead-tests",
    "addopts": "-k cache_overhead {% for node in servers %} --node={{ node.host }}:{{ node.port }}:{{ node.group }}{% endfor %}"
}
