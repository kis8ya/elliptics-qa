{
    "type": "pytest",
    "target": "elliptics/routing-table/test_base_cases.py",
    "addopts": "{% for node in servers %} --node={{ node.host }}:{{ node.port }}:{{ node.group }}{% endfor %} --backends-number={{ backends_number }} --check-timeout={{ check_timeout }}"
}
