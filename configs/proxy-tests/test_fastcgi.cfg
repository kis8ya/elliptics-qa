{
    "params": {
        },
    "addopts": "--proxy=http://{{ clients[0].host }}:{{ clients[0].port }}",
    "dir": "proxy-tests/fastcgi-tests",
    "test_env_cfg": {
        "clients": {
            "count": 1,
            "flavor": "m1.small"
            },
        "servers": {
            "count_per_group": [1, 1, 1],
            "flavor": "m1.small"
            },
	"setup_playbook": "tests/setup-fastcgi-test",
	"teardown_playbook": "tests/teardown-fastcgi-test"
        },
    "tags": ["proxy"]
}
