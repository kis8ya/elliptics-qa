{
    "params": {
        "batches_number": 10,
	"files_per_batch": 1000
    },
    "addopts": "--batches-number={batches_number} --files-per-batch={files_per_batch}",
    "dir": "functional-tests/indexes-tests",
    "test_env_cfg": {
        "clients": {
            "count": 1,
            "flavor": "m1.medium"
            },
        "servers": {
            "count_per_group": [2, 2],
            "flavor": "m1.tiny"
            },
        "delete_nodes": true
        },
    "tags": ["fast", "functional", "indexes", "pull-request", "testing-packages"]
}
