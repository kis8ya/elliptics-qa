{
    "params": {
        "testgroup": "keepalive"
        },
    "addopts": "-m \"{testgroup}\" --wait_timeout=5 --check_timeout=30",
    "dir": "timeouts-tests",
    "test_env_cfg": {
        "clients": {
            "count": 1,
            "flavor": "m1.medium"
            },
        "servers": {
            "count_per_group": [1, 1, 1, 1, 1, 1],
            "flavor": "m1.tiny"
            },
        "delete_nodes": true
        },
    "tags": ["fast", "timeouts", "timeouts-keepalive", "pull-request", "testing-packages"]
}
