---
- hosts: servers
  sudo: yes
  tasks:
    - name: Wipe out Elliptics directory
      file: path={{ elliptics_dir }} state=absent

- hosts: servers
  sudo: yes
  roles:
    - ../roles/create-elliptics-directories

- hosts: clients
  sudo: yes
  tasks:
    - name: Wipe out client log
      file: path={{ client_log }} state=absent

- include: ../elliptics-start.yml
